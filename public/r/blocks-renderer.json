{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "blocks-renderer",
  "type": "registry:component",
  "title": "Blocks Renderer",
  "description": "A component to render PayloadCMS blocks dynamically.",
  "registryDependencies": [
    "https://payload.mikecebul.com/r/hello-world.json"
  ],
  "files": [
    {
      "path": "registry/new-york/components/blocks-renderer.tsx",
      "content": "import { HelloWorldBlock } from \"@/registry/new-york/blocks/hello-world/component\";\nimport type { HelloWorldType } from \"@/registry/new-york/lib/payload-types\";\n\nexport function BlocksRenderer({ blocks }: {blocks: HelloWorldType[]}) {\n  return (\n    <div className=\"space-y-8\">\n      {blocks.map((block, index) => {\n        switch (block.blockType) {\n          case \"hello-world\":\n            return (\n              <HelloWorldBlock\n                key={block.id || index}\n                title={block.title}\n                content={block.content}\n                buttonText={block.buttonText}\n                blockType=\"hello-world\"\n              />\n            );\n          default:\n            return null;\n        }\n      })}\n    </div>\n  );\n}\n",
      "type": "registry:component"
    }
  ]
}